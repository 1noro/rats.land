diff --git a/Makefile b/Makefile
new file mode 100644
index 0000000..69cc5ee
--- /dev/null
+++ b/Makefile
@@ -0,0 +1,123 @@
+# =============================================================================
+#  rats.land — Makefile
+#
+#  Toda la web se compila DENTRO de un contenedor Docker con una versión de
+#  Hugo fijada (ver HUGO_VERSION). Así el build es reproducible y no hace falta
+#  instalar Hugo en la máquina: sólo se necesita Docker.
+#
+#  Uso rápido:
+#      make            # muestra esta ayuda
+#      make server     # servidor de desarrollo con recarga en vivo
+#      make build      # compila el sitio estático en ./public
+#
+#  Todas las variables se pueden sobreescribir, p. ej.:
+#      make server PORT=8080
+#      make build HUGO_VERSION=0.165.0
+# =============================================================================
+
+# ---- Configuración (sobreescribible desde la línea de comandos) -------------
+HUGO_VERSION ?= 0.164.0
+IMAGE        ?= ratsland/hugo:$(HUGO_VERSION)
+PORT         ?= 1313
+
+# ---- Valores derivados (no tocar) -------------------------------------------
+ROOT := $(shell pwd)
+UID  := $(shell id -u)
+GID  := $(shell id -g)
+# `--tty` sólo si hay terminal interactiva (evita fallos en CI / pipes).
+TTY  := $(shell [ -t 0 ] && echo --tty)
+
+# Ejecuta el binario `hugo` dentro del contenedor con el repo montado en /src.
+# --user conserva la propiedad de los ficheros generados (public/, resources/).
+# --entrypoint hugo permite pasar los subcomandos directamente.
+DOCKER_RUN = docker run --rm --interactive $(TTY) \
+	--user $(UID):$(GID) \
+	--volume "$(ROOT):/src" \
+	--workdir /src \
+	--env HOME=/tmp \
+	--entrypoint hugo \
+	$(IMAGE)
+
+# Igual que DOCKER_RUN pero publicando el puerto para el servidor de desarrollo.
+DOCKER_SERVE = docker run --rm --interactive $(TTY) \
+	--user $(UID):$(GID) \
+	--volume "$(ROOT):/src" \
+	--workdir /src \
+	--env HOME=/tmp \
+	--publish $(PORT):$(PORT) \
+	--entrypoint hugo \
+	$(IMAGE)
+
+.DEFAULT_GOAL := help
+.PHONY: help image build server serve drafts new clean version config deploy shell pull
+
+# -----------------------------------------------------------------------------
+help: ## Muestra esta ayuda
+	@echo ""
+	@echo "  rats.land — Hugo $(HUGO_VERSION) (dentro de Docker)"
+	@echo ""
+	@echo "  Targets disponibles:"
+	@grep -E '^[a-zA-Z0-9_-]+:.*?## .*$$' $(MAKEFILE_LIST) \
+		| sort \
+		| awk 'BEGIN {FS = ":.*?## "} {printf "    \033[36m%-10s\033[0m %s\n", $$1, $$2}'
+	@echo ""
+	@echo "  Variables: HUGO_VERSION=$(HUGO_VERSION)  PORT=$(PORT)  IMAGE=$(IMAGE)"
+	@echo ""
+
+image: ## Construye la imagen Docker con la versión de Hugo fijada (si falta)
+	@docker image inspect $(IMAGE) >/dev/null 2>&1 || { \
+		echo ">> Construyendo $(IMAGE) ..."; \
+		docker build --build-arg HUGO_VERSION=$(HUGO_VERSION) -t $(IMAGE) infra; \
+	}
+
+build: image ## Compila el sitio estático de producción en ./public
+	$(DOCKER_RUN) --gc --minify --cleanDestinationDir
+	@echo ">> Sitio generado en ./public"
+
+server: image ## Servidor de desarrollo con recarga en vivo (puerto PORT, por defecto 1313)
+	$(DOCKER_SERVE) server --bind 0.0.0.0 --port $(PORT)
+
+serve: server ## Alias de `server`
+
+drafts: image ## Como `server` pero incluye borradores y contenido futuro
+	$(DOCKER_SERVE) server --bind 0.0.0.0 --port $(PORT) \
+		--buildDrafts --buildFuture --disableFastRender
+
+new: image ## Crea contenido nuevo: make new CONTENT=post/mi-post.md
+	@test -n "$(CONTENT)" || { echo "Uso: make new CONTENT=post/mi-post.md"; exit 1; }
+	$(DOCKER_RUN) new content $(CONTENT)
+
+version: image ## Muestra la versión de Hugo usada por el contenedor
+	$(DOCKER_RUN) version
+
+config: image ## Vuelca la configuración efectiva de Hugo (útil para depurar)
+	$(DOCKER_RUN) config
+
+deploy: build ## Publica en S3/CloudFront (requiere credenciales AWS en el entorno)
+	docker run --rm --interactive $(TTY) \
+		--user $(UID):$(GID) \
+		--volume "$(ROOT):/src" \
+		--workdir /src \
+		--env HOME=/tmp \
+		--env AWS_ACCESS_KEY_ID \
+		--env AWS_SECRET_ACCESS_KEY \
+		--env AWS_SESSION_TOKEN \
+		--env AWS_DEFAULT_REGION \
+		--entrypoint hugo \
+		$(IMAGE) deploy
+
+shell: image ## Abre una shell interactiva dentro del contenedor
+	docker run --rm --interactive --tty \
+		--user $(UID):$(GID) \
+		--volume "$(ROOT):/src" \
+		--workdir /src \
+		--env HOME=/tmp \
+		--entrypoint sh \
+		$(IMAGE)
+
+clean: ## Borra los artefactos generados (public/ y resources/)
+	rm -rf public resources
+	@echo ">> Limpiado public/ y resources/"
+
+pull: ## Descarga la imagen base de hugomods para la versión fijada
+	docker pull hugomods/hugo:git-$(HUGO_VERSION)
diff --git a/README.md b/README.md
index 88dcff1..1fef857 100644
--- a/README.md
+++ b/README.md
@@ -10,6 +10,26 @@ Además, esto viene porque ahora me apetece hacer una web, me apetece hacer algo
 
 Intentaré cumplir esta visión en la medida de lo posible y no parar de sentir curiosidad por cosas nuevas.
 
+## Desarrollo local
+
+La web se compila con **Hugo v0.164.0 (edición _extended_)** dentro de un
+contenedor Docker, de forma que el build es reproducible y no hace falta
+instalar Hugo en la máquina: sólo se necesita Docker.
+
+La versión de Hugo está fijada en la imagen `infra/Dockerfile` y toda la
+operativa se maneja con el `Makefile` de la raíz (autodescriptivo):
+
+```console
+$ make            # lista todos los targets disponibles
+$ make server     # servidor de desarrollo con recarga en vivo (http://localhost:1313)
+$ make build      # genera el sitio estático en ./public
+$ make new CONTENT=post/mi-post.md   # crea una entrada nueva
+$ make clean      # borra public/ y resources/
+```
+
+Variables sobreescribibles, por ejemplo: `make server PORT=8080` o
+`make build HUGO_VERSION=0.165.0`.
+
 ## Temas de HUGO que me gustan
 
 - https://themes.gohugo.io/themes/hugo-theme-nostyleplease/
diff --git a/content/post/fallen-angels-wong-kar-wai/index.md b/content/post/fallen-angels-wong-kar-wai/index.md
index 75873d9..513fa80 100644
--- a/content/post/fallen-angels-wong-kar-wai/index.md
+++ b/content/post/fallen-angels-wong-kar-wai/index.md
@@ -2,8 +2,6 @@
 title: "Fallen Angels, neo noir en Hong Kong"
 mainTitle: "Fallen Angels"
 subtitle: "Neo noir en Hong Kong"
-mainTitle: "Fallen Angels"
-subtitle: "Neo noir en Hong Kong"
 date: 2023-03-18T01:09:00+01:00
 draft: false
 # tags: ["cine", "hong kong", "Wong Kar-Wai", "neo noir", "china"]
diff --git a/hugo.toml b/hugo.toml
index 1f83cc0..9db099d 100644
--- a/hugo.toml
+++ b/hugo.toml
@@ -1,21 +1,25 @@
 baseURL = 'https://rats.land/'
-languageCode = 'es-ES'
+# `languageCode` quedó deprecada en Hugo v0.158.0; su sustituta es `locale`.
+locale = 'es-ES'
 defaultContentLanguage = 'es'
 title = 'rats.land'
 # theme = 'hugo-theme-ratsland-sans-serif'
 theme = 'hugo-theme-ratsland-bookstyle'
 enableRobotsTXT = true
 enableGitInfo = true
-Paginate = 50
 copyright = 'Esta obra está bajo una Licencia Creative Commons BY-SA-4.0. Y el código HTML y CSS bajo la GNU GPL v3.'
-rssLimit = 15
+
+# Sustituye a la antigua clave `Paginate` (deprecada en Hugo v0.128.0 y ya eliminada).
+[pagination]
+  pagerSize = 50
+
+# Sustituye a la antigua clave `rssLimit` (deprecada en favor de [services.rss]).
+[services.rss]
+  limit = 15
 
 [markup.goldmark.renderer]
 unsafe = true
 
-[author]
-  name = 'Inoro'
-
 [Params]
   webRepo = 'https://github.com/1noro/rats.land'
   description = 'rats.land'
@@ -26,9 +30,16 @@ unsafe = true
   pgpPubKey = 'https://recordratla-public-res.s3.eu-south-2.amazonaws.com/publickey.ppuubblliicc%40protonmail.com.asc'
   articleDefaultImage = 'images/article_default_img_peach.jpg'
 
+  # El antiguo bloque raíz [author] / Site.Author quedó deprecado en Hugo
+  # v0.124.0. Ahora el autor vive bajo params y se accede vía
+  # .Site.Params.author.name en las plantillas.
+  [params.author]
+    name = 'Inoro'
+
 [languages]
   [languages.es]
-    languageName = 'Spanish'
+    # `languageName` quedó deprecada en Hugo v0.158.0; su sustituta es `label`.
+    label = 'Spanish'
     weight = 1
 
 [sitemap]
diff --git a/infra/Dockerfile b/infra/Dockerfile
new file mode 100644
index 0000000..aa254df
--- /dev/null
+++ b/infra/Dockerfile
@@ -0,0 +1,23 @@
+# syntax=docker/dockerfile:1
+#
+# Imagen reproducible para compilar rats.land con una versión de Hugo FIJADA.
+#
+# Se basa en las imágenes community de hugomods (https://docker.hugomods.com),
+# concretamente en la variante «extended + git»:
+#   - extended (sin prefijo `std`): imprescindible porque el tema transpila SCSS
+#     mediante `toCSS` (libsass), disponible sólo en la edición extended.
+#   - git (sufijo `-git`): imprescindible porque hugo.toml usa
+#     `enableGitInfo = true`, que necesita el binario de git en el contenedor.
+#
+# La versión se controla con el ARG HUGO_VERSION (ver el Makefile de la raíz).
+
+ARG HUGO_VERSION=0.164.0
+FROM hugomods/hugo:git-${HUGO_VERSION}
+
+# Al montar el repositorio como volumen, su propietario (el UID del host) no
+# coincide con el del contenedor y git aborta con «detected dubious ownership»,
+# lo que rompería enableGitInfo. Marcamos cualquier ruta como segura para que
+# funcione sea cual sea el usuario con el que se ejecute el contenedor.
+RUN git config --system --add safe.directory '*'
+
+WORKDIR /src
diff --git a/layouts/favicon.ico b/layouts/favicon.ico
deleted file mode 100644
index b66e38c547aa8a97778d44bebf55908497427db2..0000000000000000000000000000000000000000
GIT binary patch
literal 0
HcmV?d00001

literal 11454
zcmeHNdsIzX9R6;M7;X=d5k15-m@@LnOpH=g9+e6`a6=Lm(!+Q(ktjwF5k;a#rDDRZ
z+f9!<b@CV*ugPNwdChpgnZIYB>eg{?b5{2>)|xfzoYvRb`}h5R-|y_NeePO!uK*GG
znKuvQcUR<@1Ns046Oa(92<SN1B@&6Cp`ihdPMy$2Qxlrqx}m#Rj2>EA5Nm5gtfK=h
zU0rAs=;-!@uAUxx_U;WmeSPTn?TbDJ1~4!*L_Z@V^fxv}e-jfJ4j2F<0%KEC7!Mo>
zlR<-EYGwwrp+hm)+#EwJhQZv@5*9Q@jKHvwqhM)eh0)g5uo^oS);6{nXJ?1;_Vxr5
zVKZqGY#kg395KPs33deblPAN$*%^*5E^u;jg_El*rntFbs=GU;xqD!mhX<Tz%*1q0
zPXaGY_ws_9mp9zKY52gyhsJEonmd<30#AtqJ`4OX$8RC#`1?b$Xc2q^7Gr*3ApC-Y
z;TIeX|D{U_mLVV{1WQ(gVo7Ki0>i=(6dr-)t5##hnzaaxj6`@;6e0-1qt;>N`e>|*
z-hkCH8?km%ETZG%u^}M=F$sx?Nle7Xq$GlEh)v#(xa~Wzc?UsC3bv%|#MWJ@h)+u+
z*o~z0bR=hF5bQ%rW)^lG$VMtbT6PZ759T2~FCTmJX%rx%pa5BgM{wZiF=QW;B1c+;
zoT6gn(kPZ8Pga7w5;^kar6?#pjzh=GaJY=1yc~rUl{iwVfV8R_(rOCDH8lh$AR{O_
zaT4-Vr%_7d^e-qoa|Y#SYf({K3&pwfsJeIwHNRa(O<f(qWt^aK`3g>6xr$RXu3p2L
z>o;)rMm=ilZ{pXRw{Y&(ZJfW|fC~+GaN*8fTqL-3?>_48KS13B8h_x*pAT{M;Uipo
z^ccTCeuC>yp5pq`XShL7|Li&HpTEFO8ZTbr*2`D8P2<&TG&JH38eYG_9U5=m;x3K1
z?{JUCyZ5+H<NaS;uZX}24hRki4hRnXUmWOcVLRSxNH6wsL`GkZ9%nm7j~!Qc?vf=t
zUMiPKb9er*CM3{ro@7D5((vfy%%6&7^3pQ-;T7r%vhw37?o0j|t3b_Z7`HsNU!<lW
zE4^3n9@#&(sK{M4l$EdkR5Ws7a$7AFKYr(|t-f5F6_d7Cu@+I1Ftc}?sr-UEYd+9d
zBsz4^clKlSN@X-CThg7)cX%#g=tp(#u$+p(7{*^<vI^Vrz%WJ20u*CAE~lcv)VEe)
zJ01}9xjhv85&YqHfSVQekKhlt1Kg~ze*}NH9pGk#{Ui9p?Ep6`?4Ne~^Ygddsk~T*
z|I|cY3fx6sF}@#paHsNOziIh;L{R~}6u67r82W3uQ+cue4E;1sUJ{%|-!t@OBREs}
zkX`pO^r;9w5}Y-|%bEIxmYm5v$dycairPJpr6KOC!HTFgwK@8sK9%9bsMmGfurE7l
z8vi2|#{(>RiYi(?vbn?OzNw982hzFx8t6}rI`*Ob;R<?wB!1D<aT9zt7F0L0Qg_Ch
z+C7||+o;CuusNdn!=h3qXMAi>Sp(Xw+LCCJ&s1(2ZxhF;2I<JMma=vazG&lYF9qV*
zHXCI3Y`3m5vB#!L6{&Qf`+BA^Eg3vco;ulu?q{{w_X*H#xx7WSbr7U{EVb@k+&34s
zYEV>`E-4pz=P_eV;}ErU8OdKq7PhM87&J1&x3bnnQL`8&Y7R%OlPWtLG;FH<bWWx+
z)O6pYa*nS?6Ak+<&2PdXQ)QXq;!Ne+MLO&mrHu2tcbUGc@i_|zwbu9Zipy`@N{Wo2
z=4G{#YLB!e^y^k^#w--|8SOdWcZT`DbJ%ShDW~N0f0cZ+Yf93N{vS<T5EdK|91t83
R91t8391t839QbcI@Hc=lpNIee

diff --git a/layouts/robots.txt b/layouts/robots.txt
index dfda277..6b4c001 100644
--- a/layouts/robots.txt
+++ b/layouts/robots.txt
@@ -1,5 +1,4 @@
 User-agent: *
 Allow: /
 
-Sitemap: https://recod.rat.la/sitemap.xml
-Sitemap: https://recod.rat.la/image-sitemap.xml
+Sitemap: {{ "sitemap.xml" | absURL }}
diff --git a/themes/hugo-theme-ratsland-bookstyle/layouts/_default/baseof.html b/themes/hugo-theme-ratsland-bookstyle/layouts/_default/baseof.html
index 9f1b678..f4537f1 100644
--- a/themes/hugo-theme-ratsland-bookstyle/layouts/_default/baseof.html
+++ b/themes/hugo-theme-ratsland-bookstyle/layouts/_default/baseof.html
@@ -1,5 +1,5 @@
 <!DOCTYPE html>
-<html lang="{{ $.Site.LanguageCode }}">
+<html lang="{{ $.Site.Language.Locale }}">
 	<head >
 		{{- partial "head.html" . -}}
 	</head>
diff --git a/themes/hugo-theme-ratsland-bookstyle/layouts/_default/single.html b/themes/hugo-theme-ratsland-bookstyle/layouts/_default/single.html
index 0e6a95a..b47de21 100644
--- a/themes/hugo-theme-ratsland-bookstyle/layouts/_default/single.html
+++ b/themes/hugo-theme-ratsland-bookstyle/layouts/_default/single.html
@@ -13,7 +13,7 @@
 		</div>
 		{{ if in .Permalink "/post/" }}
 		<div class="post__meta">
-			<span class="post__byline">{{ with .Params.author }}<a href="/{{ . | urlize }}">{{ . }}</a>{{ else }}<a href="/{{ .Site.Author.name | urlize }}">{{ .Site.Author.name }}</a>{{ end }}</span> · <time datetime="{{ .Date.Format "2006-01-02T15:04:05-07:00" }}">{{ .Date | time.Format ":date_long" }}</time><br><span class="post__tags" aria-label="Etiquetas">{{ range (.GetTerms "tags") }}&nbsp;<a href="{{.RelPermalink}}">{{ .LinkTitle }}</a>&nbsp;{{end}}</span>
+			<span class="post__byline">{{ with .Params.author }}<a href="/{{ . | urlize }}">{{ . }}</a>{{ else }}<a href="/{{ .Site.Params.author.name | urlize }}">{{ .Site.Params.author.name }}</a>{{ end }}</span> · <time datetime="{{ .Date.Format "2006-01-02T15:04:05-07:00" }}">{{ .Date | time.Format ":date_long" }}</time><br><span class="post__tags" aria-label="Etiquetas">{{ range (.GetTerms "tags") }}&nbsp;<a href="{{.RelPermalink}}">{{ .LinkTitle }}</a>&nbsp;{{end}}</span>
 		</div>
 		{{ end }}
 		{{ if and .Params.summary (not (eq .Params.displaySummary false)) }}
diff --git a/themes/hugo-theme-ratsland-bookstyle/layouts/partials/head.html b/themes/hugo-theme-ratsland-bookstyle/layouts/partials/head.html
index ca2709e..9999981 100644
--- a/themes/hugo-theme-ratsland-bookstyle/layouts/partials/head.html
+++ b/themes/hugo-theme-ratsland-bookstyle/layouts/partials/head.html
@@ -42,7 +42,7 @@
 {{ with .Params.author }}
 	<meta name="author" content="{{ . }}">
 {{ else }}
-	<meta name="author" content="{{ .Site.Author.name }}">
+	<meta name="author" content="{{ .Site.Params.author.name }}">
 {{ end }}
 
 <meta name="theme-color" content="#000000"> <!-- Para que el navegador sepa que color debe usar en el marco -->
@@ -53,7 +53,7 @@
 	{{ with .Params.author }}
 		<meta property="article:author" content="{{ . }}">
 	{{ else }}
-		<meta property="article:author" content="{{ .Site.Author.name }}">
+		<meta property="article:author" content="{{ .Site.Params.author.name }}">
 	{{ end }}
 	<meta property="article:published_time" content="{{ .Date.Format "2006-01-02T15:04:05-07:00" }}">
 {{ else }}
@@ -62,7 +62,7 @@
 
 <meta property="og:url" content="{{ .Page.Permalink }}">
 <meta property="og:site_name" content="{{ .Site.Title }}">
-<meta property="og:locale" content="{{ .Site.LanguageCode }}">
+<meta property="og:locale" content="{{ .Site.Language.Locale }}">
 
 {{ if not .IsHome }}
 	<meta property="og:title" content="{{ .Title }} - {{ $.Site.Title }}">
diff --git a/themes/hugo-theme-ratsland-sans-serif/layouts/_default/baseof.html b/themes/hugo-theme-ratsland-sans-serif/layouts/_default/baseof.html
index 5076378..5067ffc 100644
--- a/themes/hugo-theme-ratsland-sans-serif/layouts/_default/baseof.html
+++ b/themes/hugo-theme-ratsland-sans-serif/layouts/_default/baseof.html
@@ -1,5 +1,5 @@
 <!DOCTYPE html>
-<html lang="{{ $.Site.LanguageCode }}">
+<html lang="{{ $.Site.Language.Locale }}">
 	<head >
 		{{- partial "head.html" . -}}
 	</head>
diff --git a/themes/hugo-theme-ratsland-sans-serif/layouts/_default/single.html b/themes/hugo-theme-ratsland-sans-serif/layouts/_default/single.html
index e3959c6..c12f5d7 100644
--- a/themes/hugo-theme-ratsland-sans-serif/layouts/_default/single.html
+++ b/themes/hugo-theme-ratsland-sans-serif/layouts/_default/single.html
@@ -10,7 +10,7 @@
 					Por {{ with .Params.author }}
 					<a href="/{{ . | urlize }}">{{ . }}</a>
 					{{ else }}
-					<a href="/{{ .Site.Author.name | urlize }}">{{ .Site.Author.name }}</a>
+					<a href="/{{ .Site.Params.author.name | urlize }}">{{ .Site.Params.author.name }}</a>
 					{{ end }}
 				</span> · <time datetime="{{ .Date.Format "2006-01-02T15:04:05-07:00" }}">{{ .Date | time.Format ":date_long" }}</time> · <span class="post_tags" aria-label="Etiquetas">
 					{{ range (.GetTerms "tags") }}
diff --git a/themes/hugo-theme-ratsland-sans-serif/layouts/index.html b/themes/hugo-theme-ratsland-sans-serif/layouts/index.html
index 9629d01..13bf32b 100644
--- a/themes/hugo-theme-ratsland-sans-serif/layouts/index.html
+++ b/themes/hugo-theme-ratsland-sans-serif/layouts/index.html
@@ -14,7 +14,7 @@
 					Por {{ with .Params.author }}
 					<a href="/{{ . | urlize }}">{{ . }}</a>
 					{{ else }}
-					<a href="/{{ .Site.Author.name | urlize }}">{{ .Site.Author.name }}</a>
+					<a href="/{{ .Site.Params.author.name | urlize }}">{{ .Site.Params.author.name }}</a>
 					{{ end }}
 				</span> · <time datetime="{{ .Date.Format "2006-01-02T15:04:05-07:00" }}">{{ .Date | time.Format ":date_long" }}</time> · <span class="post_tags" aria-label="Etiquetas">
 					{{ range (.GetTerms "tags") }}
